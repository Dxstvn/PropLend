/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS 4.x PostCSS plugin (includes autoprefixer)
    '@tailwindcss/postcss': {},
  },
};

export default config;
